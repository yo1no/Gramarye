package com.yo1no.gramarye;

import com.yo1no.gramarye.magic.trigger.type.TriggerPayload;

/** Empty V0 payload for the canonical active-cast trigger. */
enum P9ActiveCastTriggerPayloadV0 implements TriggerPayload {
    INSTANCE
}
