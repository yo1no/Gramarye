package com.yo1no.gramarye.magic.definition.validation;

/** Bounded reason for using presentation fallback data in a validated projection. */
public enum AppearanceFallbackReason {
    UNPARSED,
    REJECTED_DEPTH_LIMIT,
    REJECTED_NODE_LIMIT
}
