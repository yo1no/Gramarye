package com.yo1no.gramarye.magic.definition.document;

/** Known appearance field category; unknown field names are deliberately never retained. */
public enum AppearanceField {
    PRIMARY_ARGB,
    SECONDARY_ARGB,
    SOUND_PROFILE,
    PARTICLE_PROFILE,
    TRAIL_PROFILE,
    INTENSITY_MILLI
}
