package com.yo1no.gramarye.magic.definition.inspection;

/** Source actually selected by a typed payload; distinct from descriptor SourceRequirement. */
public enum SourceSelection {
    NONE,
    CURRENT_EVENT,
    PRIOR_NODE
}
